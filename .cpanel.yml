---
# TICH ERP - cPanel Git deployment (HostPinnacle)
# Repository Path MUST be: /home3/tichafri/tich-erp  (repo root, not web/)
# After push: Update from Remote → Deploy HEAD Commit
# Logs: deploy/cpanel/last-deploy.log , deploy/cpanel/last-asset-sync.log

deployment:
  tasks:
    - /bin/bash deploy/cpanel/deploy.sh
